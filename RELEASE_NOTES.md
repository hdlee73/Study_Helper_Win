## Study Helper 2.2.0-rc.1

Windows x64 / Python 3.14.7 기반 기능 개선 시험판입니다. 이번 버전은 **MP3 → Excel 번역 오류 안정화**, **MP3 → DOCX 전사문**, **UI 전면 개선**에 초점을 맞췄습니다.

### 다운로드

- **StudyHelper-Setup-2.2.0-rc.1.exe**: 설치 마법사. 일반 사용자에게 권장합니다.
- **StudyHelper-Windows-x64-2.2.0-rc.1.zip**: 압축 해제 후 `StudyHelper.exe` 실행. 전체 폴더를 유지하세요.
- **SHA256SUMS.txt**: 다운로드 파일 무결성 확인용 해시.
- **build-validation.json / source-validation.json**: 자동 검증 결과.

### 주요 변경사항

#### 1. MP3 → Excel 번역 요청 제한 오류 수정

기존 버전은 문장 번역을 최대 4개 스레드에서 동시에 호출해 Google 번역 무료 엔드포인트의 초당 요청 제한(오류 화면에 표시된 5 requests/second)을 짧은 시간에 넘길 수 있었습니다. 새 버전은 번역 요청을 순차 처리하고, 요청 사이에 여유 간격을 두며, 429/Too Many Requests 계열 오류에는 지수 백오프로 자동 재시도합니다.

동일 문장 번역은 메모리 캐시를 사용해 불필요한 재요청도 줄였습니다. 일일 한도 자체가 소진된 경우에는 자동 재시도 후 이해하기 쉬운 안내를 표시합니다.

#### 2. MP3 → PDF를 MP3 → DOCX로 변경

전사 결과를 편집 가능한 Word `.docx` 파일로 저장합니다. Whisper의 구간 시작/종료 시간을 보존해 타임라인을 넣을 수 있습니다.

- **타임라인 포함**: `시간 | 내용` 2열 구조로 생성합니다.
- 타임라인은 첫 번째 열에만 있으므로 Word에서 **첫 번째 열 전체 선택 → 열 삭제**로 한 번에 제거할 수 있습니다.
- **타임라인 없는 사본도 함께 저장** 옵션을 제공해, 필요하면 두 버전을 동시에 만들 수 있습니다.
- 타임라인을 처음부터 제외하고 일반 문단형 DOCX로 저장할 수도 있습니다.

#### 3. UI 개선

기존의 밝은 민트 계열 화면을 정리하고, 짙은 사이드바 + 흰색 카드 + 파란색 강조색 기반의 Windows 데스크톱 UI로 개편했습니다. 메뉴, 상태 배지, 입력 필드, 버튼, 기능 설명의 시각적 계층을 일관되게 맞췄습니다.

### 설치 전 확인

- EXE 사용 시 Python 설치는 필요 없습니다.
- FFmpeg 실행 파일은 배포본에 포함하지 않습니다. MP3 생성·변환 기능에는 FFmpeg가 필요합니다.
- YouTube에는 Deno, Anki 가져오기에는 Anki와 AnkiConnect가 추가로 필요합니다.
- 음성 인식 모델은 최초 사용 시 다운로드합니다.
- 번역 문장은 Google 번역 서비스로, TTS 문장은 Microsoft 음성 서비스로 전송됩니다.
- 코드 서명이 없는 시험판입니다.
