## Study Helper 2.2.0-rc.2

Windows x64 / Python 3.14.7 기반 기능 개선 시험판입니다. `2.2.0-rc.1`에서 일부 PC/IP 환경에 남아 있던 **MP3 → Excel Google 번역 제한 오류를 한 단계 더 우회**한 버전입니다. DOCX 전사문과 새 UI는 rc.1의 기능을 그대로 유지합니다.

### 다운로드

- **StudyHelper-Setup-2.2.0-rc.2.exe**: 설치 마법사. 일반 사용자에게 권장합니다.
- **StudyHelper-Windows-x64-2.2.0-rc.2.zip**: 압축 해제 후 `StudyHelper.exe` 실행. 전체 폴더를 유지하세요.
- **SHA256SUMS.txt**: 다운로드 파일 무결성 확인용 해시.
- **build-validation.json / source-validation.json**: 자동 검증 결과.

### rc.2 핵심 수정: 번역 경로 재설계

rc.1은 기존 deep-translator의 Google 웹 페이지 기반 번역 방식을 그대로 사용하면서 요청 간격만 늦췄습니다. 이 방식은 요청 횟수가 적더라도 Google 쪽에서 특정 IP/세션을 제한하면 계속 `Too Many Requests`/`Server Error`가 발생할 수 있었습니다.

rc.2에서는 다음 순서로 번역합니다.

1. deep-translator의 기존 웹 스크래핑 경로 대신 Google의 경량 JSON 번역 엔드포인트를 우선 사용합니다.
2. 요청 속도를 약 **1초당 1회 이하**로 제한하고 429/일시 오류에 추가 대기 후 재시도합니다.
3. Google 경로가 계속 막히면 **MyMemory 번역 서비스를 예비 경로**로 자동 시도합니다.
4. 두 경로가 모두 실패한 경우에만 기존 deep-translator 경로를 마지막 1회 시도합니다.

따라서 rc.1처럼 단순히 0.3초 간격으로 Google 웹 번역을 반복하는 구조보다 요청 수/세션 제한에 훨씬 강합니다. 외부 번역 서비스 두 곳이 모두 차단되거나 네트워크에서 해당 도메인을 막는 경우에는 오류가 남을 수 있습니다.

### MP3 → DOCX

기존 PDF 기능은 편집 가능한 Word `.docx` 전사문 기능으로 변경되어 있습니다. Whisper의 구간 시작/종료 시간을 보존해 타임라인을 넣을 수 있습니다.

- **타임라인 포함**: `시간 | 내용` 2열 구조로 생성합니다.
- 타임라인은 첫 번째 열에만 있으므로 Word에서 **첫 번째 열 전체 선택 → 열 삭제**로 한 번에 제거할 수 있습니다.
- **타임라인 없는 사본도 함께 저장** 옵션을 제공합니다.
- 타임라인을 처음부터 제외한 일반 문단형 DOCX도 만들 수 있습니다.

### UI

짙은 사이드바 + 흰색 카드 + 파란색 강조색 기반의 새 UI를 유지합니다. rc.2 배포본에서는 실행 파일에 포함된 `VERSION` 값을 읽어 창 제목과 좌측 버전 표시도 `2.2.0-rc.2`로 표시하도록 보완했습니다.

### 설치 전 확인

- EXE 사용 시 Python 설치는 필요 없습니다.
- FFmpeg 실행 파일은 배포본에 포함하지 않습니다. MP3 생성·변환 기능에는 FFmpeg가 필요합니다.
- YouTube에는 Deno, Anki 가져오기에는 Anki와 AnkiConnect가 추가로 필요합니다.
- 음성 인식 모델은 최초 사용 시 다운로드합니다.
- 번역 문장은 Google 번역 또는 장애 시 MyMemory 번역 서비스로 전송될 수 있으며, TTS 문장은 Microsoft 음성 서비스로 전송됩니다.
- 코드 서명이 없는 시험판입니다.
